This is the slag task orchestrator (Rust). Current state: 140 tests pass, 0 clippy warnings.
Acceptance: cargo test --all passes with 0 failures.

Add a test that extract_number_before returns None for empty string