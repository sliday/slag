# 100 slag improvements mined from the Claude Code source (2026-08-17)

Source: 8-beat sweep of /Users/stas/Playground/Claude Code src (140 raw ideas, curated to 100).
Status legend: [ ] todo · [~] in flight · [x] done

Progress: wave 1 (items 1-16) + item 18 in v2.2.0. Wave 2 (~12 items) + adaptive casts + site in v2.3.0. Wave 3 (15 items: compaction v3, provider resilience, founder briefing, observability) in v2.4.0. ~45/100 through v2.4.0. Wave 4 (15 items: transcripts + crash-resume + rewind checkpoints, status --json, runs/ps, OSC progress, cost persistence) in v2.5.0 — ~60/100 shipped. Wave 5 (policy engine, read-only bash classifier, full background bash, recipe frontmatter, insights, #80 reconciled) shipped 2026-08-26 — ~66/100. Assessed 2026-08-18 order, all landed:
1. ~~#99 MCP stdio client~~ shipped 2026-08-21
2. ~~#95 command policy engine~~ shipped 2026-08-26
3. ~~#97-S sleep-guard subset~~ shipped 2026-08-20; ~~full background bash~~ shipped 2026-08-26
4. ~~#100 slag insights~~ shipped 2026-08-26
5. ~~#96 read-only bash classifier~~ shipped 2026-08-26
6. ~~#98 recipe frontmatter growth~~ shipped 2026-08-26 (fork execution deferred, see item)


## Wave 1

- [x] **1. Adopt Retry-After-aware exponential backoff with jitter** (S)
  engine/provider.rs: replace BACKOFF_MS=[250,1000] fixed array with base-500ms doubling capped at 32s plus 0-25% jitter; parse the Retry-After response header on 429 and let the server directive override the computed delay.
  _evidence: Claude Code src/services/api/withRetry.ts:519-548_
- [x] **2. Broaden retry classification: 408/409/5xx, connect errors, x-should-retry** (S)
  engine/provider.rs chat_impl: extend the '429 || 5xx' check to 408/409/429/5xx plus reqwest connect/timeout errors; honor an x-should-retry header if OpenRouter sends it; expose retryable() on a structured error in error.rs.
  _evidence: Claude Code src/services/api/withRetry.ts:610-621,696-787_
- [x] **3. Stop charging heats for transient provider errors** (S)
  pipeline/forge.rs:386-404: classify SlagError::Provider into transient (429/5xx/timeout) vs permanent; transient failures get a separate counter and do NOT increment :heat or run proof/judge. Prevents network weather cracking an ingot in 20 seconds.
  _evidence: Claude Code src/query.ts:1258-1265; slag-rs/src/pipeline/forge.rs:386-404_
- [x] **4. Treat empty or truncated 200 responses as retryable** (S)
  engine/provider.rs normalize(): a 200 with empty choices, null message, or missing finish_reason must classify as a retryable Provider error inside the attempt loop instead of passing through as an empty assistant turn.
  _evidence: Claude Code src/services/api/claude.ts:2337-2364_
- [x] **5. Synthesize error tool_results for orphaned tool calls on abort** (S)
  engine/agent.rs: when cancel or a provider/tool error aborts mid-batch after tool_calls arrived, append is_error tool_results for every unanswered call before returning, so next-heat transcripts never carry dangling tool_calls (OpenAI-format backends 400 on replay).
  _evidence: Claude Code src/query.ts:123-149,984,1025_
- [x] **6. Enforce hard per-ingot and per-run spend caps** (S)
  config.rs gains max_cost_per_ingot / max_cost_per_run; agent.rs checks accumulated usage.cost each turn (engine/mod.rs:130-141 already sums it), emits a Warning event at 80% and cracks with reason 'budget' at 100%. Unattended runs need a runaway-spend guard.
  _evidence: Claude Code src/QueryEngine.ts:971-1002; src/components/CostThresholdDialog.tsx_
- [x] **7. Add a compaction circuit breaker after 3 failed shrinks** (S)
  engine/agent.rs chat_shrinking + engine/compact.rs: count consecutive compactions that fail to get below budget; after 3, crack the ingot with a clear 'context irrecoverable' error instead of halving budgets forever, and record it in the JSONL event stream.
  _evidence: Claude Code src/services/compact/autoCompact.ts:57-70,257-349; src/query.ts:1292-1296_
- [x] **8. Gate read_file on byte size before reading** (S)
  engine/tools.rs read_file: stat first; if len > 256 KB and no offset/limit given, return an error naming the size and instructing offset/limit use — the error costs ~100 bytes vs ~25K tokens of truncated content.
  _evidence: Claude Code src/tools/FileReadTool/limits.ts:1-18; src/utils/file.ts:48_
- [x] **9. Coerce stringly-typed tool args ("true", "5") before parsing** (S)
  engine/tools.rs arg helpers: as_bool silently drops a string "true" to false today. Add opt_bool/opt_u64 helpers accepting Value::String("true"/"false") and numeric strings, used by edit_file/bash/read_file. Naive truthiness is wrong ('false' -> true).
  _evidence: Claude Code src/utils/semanticBoolean.ts; src/utils/semanticNumber.ts_
- [x] **10. Interpret per-command exit codes (grep 1 = no matches, not error)** (S)
  engine/tools.rs run_shell: before appending '(exit N)', consult a static map keyed on the last pipeline segment's argv[0] (grep/rg 1='no matches', diff 1='files differ', test 1='condition false'); only >=2 is a real error. Also fixes proofs using grep/diff.
  _evidence: Claude Code src/tools/BashTool/commandSemantics.ts_
- [x] **11. Default-deny destructive bash commands with a confirm override** (S)
  engine/tools.rs bash: port the regex table (git reset --hard, push --force, clean -f, rm -rf, --no-verify, DROP TABLE, terraform destroy...); since slag forges unattended, refuse matches with the warning as the tool error plus a JSONL event; config flag to relax per crucible. Merges the advisory-annotation variant.
  _evidence: Claude Code src/tools/BashTool/destructiveCommandWarning.ts:12-102_
- [x] **12. Add a glob tool with 100-file cap and truncation notice** (S)
  engine/tools.rs: new `glob` tool (globset or `rg --files -g`), sandbox-resolved root, 100-file cap with '(Results are truncated...)' notice, paths relative to anvil root, corrected-path suggestion on missing dirs. Stops the smith burning bash calls on `find`.
  _evidence: Claude Code src/tools/GlobTool/GlobTool.ts:154-197_
- [x] **13. Warn edit_file about the read_file line-number prefix** (S)
  engine/tools.rs edit_file spec: slag's read_file emits 'LINENUM|CONTENT' but edit_file never mentions it. Add: never include the prefix in old_string/new_string; match content after the | byte-for-byte. Kills the #1 cause of failed exact-match edits.
  _evidence: Claude Code src/tools/FileEditTool/prompt.ts (getDefaultEditDescription)_
- [x] **14. Add numeric length anchors to the smith prompt** (S)
  engine/prompt.rs stable band: smiths are headless, inter-tool commentary is pure waste. Add '≤25 words between tool calls; finish summary ≤120 words'. Measured ~1.2% output-token reduction vs qualitative 'be concise'.
  _evidence: Claude Code src/constants/prompts.ts:529-537_
- [x] **15. Add the faithful-reporting rule to guard the proof gate** (S)
  engine/prompt.rs '## Rules' + finish tool description in engine/tools.rs: 'report outcomes faithfully; never claim tests pass when output shows failures; never weaken checks to manufacture green'. Pair with 'never modify the proof command or its tests unless the ingot asks'. Written against a measured 29-30% false-claim rate.
  _evidence: Claude Code src/constants/prompts.ts:237-242_
- [x] **16. Head-keep bash truncation with '[N lines truncated]' counts** (S)
  engine/tools.rs truncate_tail: switch to head-keep (or head 20% + tail 80% since build errors print last) and report the hidden line count instead of bytes; cap from SLAG_BASH_OUTPUT_CAP with a hard ceiling in config.rs.
  _evidence: Claude Code src/tools/BashTool/utils.ts:133-165; src/utils/shell/outputLimits.ts_

## Wave 2

- [x] **17. Require read-before-edit and refuse stale edits via mtime** (M)
  engine/tools.rs: ToolBox read_state: Mutex<HashMap<PathBuf,(mtime, checksum)>> updated by read/write/edit; edit_file and write_file on existing files error if never read or mtime moved since. State the enforcement in both tool descriptions ('this tool will error if...'). Protects proof-gated ingots from clobbering parallel-anvil changes.
  _shipped: engine/tools.rs `FileState{mtime, checksum, seq}` + `read_state` map (checkbox flipped after audit): write_file/edit_file on an existing file refuse unless it was read this session and its mtime has not moved — "refused stale write; {raw} changed on disk since your last read". Protects proof-gated retries from clobbering parallel-anvil edits._
  _evidence: Claude Code src/tools/FileEditTool/FileEditTool.ts:275-306; src/tools/FileWriteTool/FileWriteTool.ts:190-216_
- [x] **18. Return a 'file unchanged' stub on repeated identical reads** (M)
  engine/tools.rs read_file: keep per-session map path -> (mtime, hash, offset/limit); on identical re-read return 'File unchanged since last read — refer to the earlier tool_result' instead of contents. Big token win in long forge loops.
  _evidence: Claude Code src/tools/FileReadTool/prompt.ts (FILE_UNCHANGED_STUB); FileReadTool.ts:686-691_
- [x] **19. Spill oversized tool results to disk with preview + path** (M)
  engine/tools.rs run_shell + engine/agent.rs: when merged output exceeds BASH_OUTPUT_CAP, write full output to logs/tool-results/<call_id>.txt inside the anvil, return head preview + 'full output saved to <relative path>' so the smith can grep it. Also apply to grep overflow.
  _evidence: Claude Code src/utils/toolResultStorage.ts; src/Tool.ts:466_
  _shipped: `tools.rs` `spill_tool_result()` writes full output to `logs/tool-results/<hash>.txt` inside the anvil and returns the workspace-relative path, so the smith reaches it with the same sandboxed `read`/`grep` it uses for source. Two cut sites needed it, not one: `run_shell` (past `bash_output_cap()`, covering bash and glob, which both route through it) and `grep`'s own `head_limit` tail-drop, which happens on lines already in memory after `run_shell` returned and is therefore invisible to the first spill. Filenames are content-addressed rather than call-id-keyed — `run_shell` has no call id at hand, and hashing means a command re-run across heats reuses one file instead of growing the anvil every retry. Best-effort: a spill that cannot be written degrades to plain truncation. The two cuts are reconciled rather than stacked: when a grep blows the bash cap *and* the line limit, only the first spill holds the pre-cap output, so the tail cut names that file instead of writing a second, smaller one and calling it full — and the bash-cap note is filtered out of the match lines so it cannot pad the `(truncated: N more lines)` count — keyed on the note's `SPILL_NOTE_PREFIX` opening rather than the directory name, so a real hit that quotes `logs/tool-results` is not discarded as bookkeeping. Tests: `oversized_bash_output_spills_to_disk_and_names_the_file`, `output_within_the_cap_does_not_spill`, `oversized_grep_output_spills_through_the_same_path`, `a_doubly_truncated_grep_names_one_honest_spill`, `a_match_that_quotes_the_spill_path_is_not_swallowed`, `identical_oversized_output_reuses_one_spill_file`._
- [x] **20. Upgrade grep: output modes, context lines, head_limit, relative paths** (M)
  engine/tools.rs grep: add output_mode (default files_with_matches via rg -l), -A/-B/-C context, -i, glob filter, and head_limit passed through to rg; relativize paths to the anvil root instead of absolute resolved paths.
  _shipped: engine/tools.rs grep spec: `output_mode` enum (files_with_matches default / content / count), `-A`/`-B`/`-C` context lines in content mode, `-i` case-insensitivity, a `glob` file filter, and `head_limit` capping output at the first N lines (default 100). The search runs from the anvil root, so hits print workspace-relative._
  _evidence: Claude Code src/tools/GrepTool/GrepTool.ts:52-107,316-365_
- [x] **21. Trigger compaction on usage-anchored token counts, not chars** (M)
  engine/agent.rs: provider.rs already parses OpenRouter usage; store last-response prompt/total tokens on the session, add chars/4 estimates only for messages appended since (never cumulative sums — they double-count), and drive compact() from tokens.
  _shipped: engine/agent.rs `TokenAnchor` + `estimate_tokens()`: the provider's reported prompt_tokens anchors the count and only messages appended since are estimated at chars/4; the anchor is dropped when history is pruned. `compact_to_tokens()` drives compaction off that estimate. Covered by `token_estimate_prefers_the_usage_anchor_over_chars` and `usage_anchor_triggers_compaction_before_chars_would`._
  _evidence: Claude Code src/utils/tokens.ts:200-261; services/compact/microCompact.ts:164-205_
- [x] **22. Derive context budget from model window minus output reserve** (M)
  engine/agent.rs char_budget_from_env is a fixed constant; fetch context_length per model from OpenRouter /models (provider.rs) and compute budget = window − output_reserve − compact_buffer, keeping SLAG_CHAR_BUDGET as override.
  _shipped: engine/agent.rs `budget_for_window()`: window − `OUTPUT_RESERVE_TOKENS` (16384) − `COMPACT_BUFFER_TOKENS` (8192), plumbed through `NativeAgent::with_context_window()`; `SLAG_CHAR_BUDGET` still overrides._
  _evidence: Claude Code src/services/compact/autoCompact.ts:28-91_
- [x] **23. Prune at API-round boundaries to keep tool pairing intact** (M)
  engine/compact.rs: add a round-grouping helper over ChatMessage (assistant-with-tool_calls starts a group, its tool results belong to it) and make all drops operate on whole rounds — OpenAI-format providers on OpenRouter 400 on orphan tool messages.
  _shipped: engine/compact.rs `group_rounds()` (assistant-with-tool_calls opens a round, its tool results belong to it) with every drop routed through `drop_candidate()`, which only returns whole rounds and never one reaching into the protected tail. Guarded by `assert_no_orphans` in the compaction tests._
  _evidence: Claude Code src/services/compact/grouping.ts:22-63_
- [x] **24. Stub only replayable read-only tool results when pruning** (S)
  engine/compact.rs prunable(): key stub-eligibility on tool name (read/bash/grep are re-runnable; keep edit results and proof outputs), keep the last result per file path, and floor keepRecent at 1 so the model never loses all working context.
  _shipped: engine/compact.rs `stub_eligible()` keys on tool name (read_file, grep, glob, bash, recipe_view are replayable; edit/write confirmations stay), `stub_candidate()` keeps the newest result per file path when duplicates exist, and skips `last_tool` so keepRecent floors at 1._
  _evidence: Claude Code src/services/compact/microCompact.ts:40-50,456-463_
- [x] **25. Standardize a <system-reminder> envelope for all injections** (M)
  engine/prompt.rs + agent.rs: one wrap_reminder() helper for steer injections, proof-failure notices, workspace refreshes, truncation/staleness notes, with the 'may or may not be relevant' disclaimer; declare the contract once in the stable band; coalesce consecutive reminders. Merges the tool-result advisory and injected-context variants.
  _shipped: engine/prompt.rs `wrap_reminder()` / `append_reminder()` + the `REMINDER_OPEN`/`REMINDER_CLOSE` constants: one envelope for every out-of-band injection. `append_reminder` coalesces into a reminder already at the carrier's tail instead of stacking a second identical tag. Call sites: agent.rs steer injection (into the last tool result, or a coalescing user message before any tool ran) and compact.rs post-compaction file re-injection. The contract — context not a user turn, act on it without acknowledging it, "may or may not be relevant" — is declared once in the stable band under `## System reminders`._
  _evidence: Claude Code src/constants/prompts.ts:131-134,186-197; src/utils/messages.ts:3098; src/utils/api.ts:460-472_
- [x] **26. Add a git safety protocol to the bash spec** (S)
  engine/tools.rs bash spec / engine/prompt.rs: never --no-verify; never amend after a pre-commit hook failure (the commit did NOT happen — amend destroys the previous commit); stage specific files over `git add -A`; heredoc commit messages; no -i flags. Anvils commit in worktrees, so a botched amend poisons the merge in anvil/worktree.rs.
  _evidence: Claude Code src/tools/BashTool/prompt.ts:88-140_
  _shipped: tools.rs bash spec (landed with the wave-2 backlog batch, commit 97456f7; checkbox flipped now): never --no-verify; after a pre-commit hook failure the commit did NOT happen, so fix and commit again (never amend); stage specific files over 'git add -A'; heredoc commit messages; no interactive -i flags._
- [x] **27. Add the minimal-uniqueness hint for old_string** (S)
  engine/tools.rs edit_file description: 'use the smallest old_string that is clearly unique — 2-4 adjacent lines usually suffice' plus the not-unique failure note (add context or replace_all). Fewer whitespace-drift mismatches feeding the fuzzy ladder.
  _evidence: Claude Code src/tools/FileEditTool/prompt.ts (minimalUniquenessHint)_
  _shipped: tools.rs edit_file spec: 'use the smallest old_string that is clearly unique — 2-4 adjacent lines usually suffice; when a match is not unique, add adjacent context lines or set replace_all'; spec test edit_file_spec_carries_minimal_uniqueness_hint._
- [x] **28. Return precise warnings for empty files and past-EOF offsets** (S)
  engine/tools.rs read_file: on empty content return the 'file exists but contents are empty' reminder; on offset past EOF return 'file has M lines' so the model corrects its next call instead of guessing.
  _evidence: Claude Code src/tools/FileReadTool/FileReadTool.ts:703-708_
  _shipped: tools.rs read_file (landed with the v1.2.0 native engine, commit 09cebbf; checkbox flipped now): empty content returns '(empty file)'; an offset past EOF returns '(file has M lines; offset N is past the end)'; oversized files also report their line count beside the size-limit refusal._
- [x] **29. Teach parallel-vs-sequential command batching in the bash spec** (S)
  engine/tools.rs bash spec: independent commands -> multiple tool calls in ONE message; dependent -> chain with '&&'; ';' only when earlier failures don't matter; no newline-separated commands. agent.rs already dispatches parallel tool calls — this wording makes the dispatcher get used.
  _evidence: Claude Code src/tools/BashTool/prompt.ts (multipleCommandsSubitems)_
  _shipped: tools.rs bash spec (wave-2 batch, commit 97456f7; checkbox flipped now): independent commands as multiple bash calls in ONE message (agent.rs dispatches tool calls in parallel), dependent commands chained with '&&', ';' only when earlier failures don't matter, never newline-separated._
- [x] **30. Add anti-gold-plating code-style rules to the smith prompt** (S)
  engine/prompt.rs stable band: no features/refactors/comments beyond the ask; validate only at system boundaries; 'three similar lines beat a premature abstraction'; don't remove existing comments. Smaller diffs mean faster duel judging and fewer proof-adjacent regressions.
  _evidence: Claude Code src/constants/prompts.ts:200-214_
  _shipped: engine/prompt.rs stable band '### No gold-plating' (wave-2 batch, commit 97456f7; checkbox flipped now): only what the ingot asks, validate inputs only at system boundaries, 'three similar lines beat a premature abstraction', never remove existing comments; guarded by test stable_band_carries_anti_gold_plating_rules._
- [x] **31. Append 'did you mean' path suggestions on file-not-found** (S)
  engine/tools.rs resolve()/read_file errors: on 'cannot read' or 'escapes workspace', append the anvil root and probe for the same basename/relative tail under the root (single walkdir pass) as a hint. Cuts a whole retry turn per wrong-path guess.
  _evidence: Claude Code src/utils/file.ts:213,228-263; src/tools/GlobTool/GlobTool.ts:109-119_
  _shipped: tools.rs path_hint(): resolve() escape errors and read_file cannot-read errors append the workspace root plus up to three same-basename matches from a bounded walk (skips .git/target/node_modules/.venv and symlinked dirs, ~4000-entry cap) as 'did you mean: …'. Three tests cover the suggestion, the no-false-suggestion case, and root naming on escape errors._
- [x] **32. Raise the retry budget and make it env-overridable** (S)
  engine/provider.rs: MAX_ATTEMPTS=3 becomes config/env driven (SLAG_MAX_RETRIES, default ~8-10) now that backoff is exponential; three attempts over 1.25s is far too brittle for overnight forge runs.
  _evidence: Claude Code src/services/api/withRetry.ts:52,789-797_
  _shipped: provider.rs (landed with the narrator batch, commit 51cd886; checkbox flipped now): SLAG_MAX_RETRIES overrides DEFAULT_MAX_ATTEMPTS=8 on the exponential 500ms→32s curve; unattended 429/529 retries ride past the budget under a 6h ceiling; tests configured_retry_budget_bounds_the_attempts + plan_retry_bounded_path_respects_the_budget._
- [x] **33. Emit structured api_retry heartbeat events** (M)
  engine/events.rs: add EngineEvent::ApiRetry{attempt,max,delay_ms,status}; provider.rs takes an optional EventTx to emit before each sleep; dashboard.rs renders a countdown on the anvil row instead of a silent hang.
  _shipped: engine/events.rs `EngineEvent::ApiRetry{attempt, status, remaining_secs}` emitted from provider.rs before each backoff sleep and rendered as a countdown on the anvil row instead of a silent hang._
  _evidence: Claude Code src/services/api/withRetry.ts:466-511; src/QueryEngine.ts:943-955_
- [x] **34. Compute pricing locally when OpenRouter omits cost, flag estimates** (M)
  engine/provider.rs: when usage.cost is None, compute from a pricing map fetched once from OpenRouter GET /models and cached in the config dir; mark Usage estimated:bool so dashboard/assay print '~$0.0123 (est)'.
  _shipped: new `engine/pricing.rs` — `PricingTable`/`parse_table`/`lookup`/`estimate` with the variant-suffix fallback the window cache uses (`…:nitro` → base), minus `:free`, which bills nothing and so must never inherit the paid base rate; a `pricing.json` disk cache in the config dir under a 24h TTL; and `format_cost()`. provider.rs `attribute()` fills a missing `usage.cost` from the table and sets `Usage.estimated`, so dashboard.rs prints `~$0.0123 (est)`. One `/models` fetch now feeds both caches: `ModelsIndex` (windows + prices) replaced the per-model window map, so adding prices cost zero extra requests. A "-1" (variable) price is dropped rather than stored, and a free model estimates to `None` instead of a `$0.0000 (est)` line._
  _evidence: Claude Code src/utils/modelCost.ts:104-163; src/cost-tracker.ts:228-234_
- [x] **35. Split the cost ledger per model and per role** (M)
  dashboard.rs: state.totals becomes HashMap<(model, role), Usage> fed by EngineEvent::Tokens (add model+role fields in engine/mod.rs); pipeline/assay.rs prints smith vs judge vs founder vs surveyor rows. Duel/judge spend becomes visible.
  _shipped: `engine/pricing.rs` `CostLedger` (a `BTreeMap<(String, Role), Usage>`, `const fn new()` so it lives in the `engine::stats` static cell) folded by `stats::record_usage()` inside provider.rs `attribute()` — the single success path out of `chat()`, chosen because the judge and the summarizer hold a provider directly and a fold in the agent loop would drop their rows — rendered by `stats::ledger_lines()` and printed under a `spend` heading in `pipeline/assay.rs`. Attribution rides on `Usage.model`/`Usage.role` rather than on new `EngineEvent::Tokens` fields, so every existing `Tokens { usage }` match arm kept compiling and old JSONL still deserializes. Roles are set where the smith is built: `NativeSmith::forge` → Smith, the duel-cast constructor → Duel, `make_plan_smith(_, _, role)` → Surveyor or Founder (they share a model, so only the role separates them), judge.rs → Judge, compact.rs → Compact._
  _evidence: Claude Code src/cost-tracker.ts:181-226,250-276,304-322_
- [x] **36. Show OpenRouter credit balance beside session spend** (S)
  Add `slag cost` (cli.rs): current run ledger plus OpenRouter GET /api/v1/credits via provider.rs's HTTP client — 'session $0.42 · account $18.31 remaining'; warn at forge start when balance < configured floor.
  _shipped: provider.rs `Credits`/`parse_credits`/`fetch_credits` against `GET /credits` (shaped like the existing `check_key`), `Command::Cost` + `cli::show_cost()` printing `cost_line()` — `session $0.42 · account $18.31 remaining`. Either half degrades on its own: an unreachable balance endpoint still prints the session spend, and a run that has spent nothing reads `session -` rather than a `$0.00` that would look like fact. `config::credit_floor()` (`SLAG_CREDIT_FLOOR` / `credit_floor`) drives a forge-start warning that never blocks, since a best-effort balance lookup must not stop a forge._
  _evidence: Claude Code src/commands/usage/usage.tsx; src/commands/extra-usage/extra-usage-core.ts:33-51_
- [x] **37. Add an <env> block with model identity and knowledge cutoff** (S)
  engine/prompt.rs workspace_snapshot(): <env> sub-block with platform/shell/OS via std::env + os_info, 'You are powered by <OpenRouter slug>', and a small cutoff table for routed model families. Stable within a session, so cache-safe in the context band.
  _evidence: Claude Code src/constants/prompts.ts:606-756_
  _shipped: engine/prompt.rs workspace snapshot <env> block (wave-2 batch, commit 97456f7; checkbox flipped now): platform/shell/OS, 'You are powered by <model>', a knowledge-cutoff table for the routed families, and a verify-don't-guess line for anything newer; stable within a session, so cache-safe._
- [x] **38. Register central cleanup handlers plus a panic hook** (S)
  New shutdown.rs: static registry of boxed cleanups (flush JSONL sink, save crucible under CRUCIBLE_LOCK, restore ratatui terminal) invoked from a ctrl-c handler and a panic hook in main.rs so a panic never leaves the terminal in raw mode or drops buffered events.
  _evidence: Claude Code src/utils/cleanupRegistry.ts; src/utils/gracefulShutdown.ts_
  _shipped: new src/shutdown.rs — a process-global registry of boxed `FnOnce() + Send` cleanups drained by exactly two triggers, both installed from main.rs before anything can register: `install_panic_hook()` (runs cleanups, then the previous hook, so a backtrace lands on a restored terminal) and `install_signal_handler()` (shell Ctrl-C, then exit 130). Registrations run last-in-first-out — the terminal is claimed last and released first — each inside `catch_unwind`, so a buggy cleanup cannot strand the rest; `run_all` drains, so a Ctrl-C arriving mid-panic never double-saves. Registered chores: dashboard.rs `register_terminal_restore()` (raw mode, alternate screen, OSC title + progress pip), `register_crucible_rescue()` (reload, hand Molten ingots back to Ore, save — skipped when CRUCIBLE_LOCK is held, since the holder's write is fresher), and the `slag ps` PID entry. The JSONL sink needs no flush chore: `spawn_jsonl_sink` already flushes per line, so no event is ever buffered at exit. Tests: `cleanups_run_in_reverse_registration_order`, `draining_is_idempotent`, `a_panicking_cleanup_does_not_strand_the_others`, `pending_reports_registry_depth` — the registry proper. The two triggers install process-global state and no test touches them: `install_panic_hook()` and `install_signal_handler()` are exercised only by running the binary._
- [x] **39. Require double-press Ctrl-C with a 'press again to cancel' hint** (S)
  dashboard.rs handle_key: first Ctrl-C sets pending_cancel: Option<Instant> and flashes a hint; second press within 800ms stores CancelFlag. Protects long forges from an accidental cancel.
  _evidence: Claude Code hooks/useDoublePress.ts (DOUBLE_PRESS_TIMEOUT_MS=800)_
  _shipped: dashboard.rs `handle_key_at()` — the first Ctrl-C only stores `pending_cancel: Option<Instant>` and returns Stay; a second press within DOUBLE_PRESS (800ms) stores the CancelFlag and returns Cancel. A lapsed arm re-arms instead of cancelling (two strays a second apart are two accidents), and any other keystroke disarms, so a stray press cannot pair with one from minutes earlier. While armed, draw_bottom() flashes 'press Ctrl-C again to cancel the forge' in WARM. `handle_key` is now a thin wrapper over the clock-injected form, so the window is tested without sleeping. Tests: `a_single_ctrl_c_only_arms_the_cancel`, `a_second_ctrl_c_inside_the_window_cancels`, `a_lapsed_arm_starts_over_instead_of_cancelling`, `typing_after_a_stray_ctrl_c_disarms_it`, `an_armed_cancel_flashes_the_press_again_hint`._
- [x] **40. Tint stalled anvils red after seconds without tokens** (S)
  dashboard.rs: store last-token Instant per forging IngotRow (updated on Tokens/ToolResult); tint the row yellow after 15s of silence and red after 60s with a '(stalled Ns)' suffix. Pure display change, instant 'is it hung?' answer.
  _evidence: Claude Code components/Spinner/useStalledAnimation.ts (3000ms threshold)_
  _shipped: dashboard.rs ingot_line(): each forging row carries a `last_activity` Instant bumped on Tokens/ToolResult; past STALL_WARN (15s) the row tints BRIGHT (yellow) with a `(stalled Ns)` suffix, past STALL_DEAD (60s) it goes WARM (red). Display only. Checkbox flipped after reading the code against the spec. Tests: `stalled_forging_rows_tint_yellow_then_red`, `stall_tint_only_applies_to_forging_rows`, `tokens_and_tool_results_refresh_only_forging_rows`, `has_stalled_detects_silent_forging_rows`._
- [x] **41. Show queued steer messages as a capped persistent list** (S)
  dashboard.rs: replace the 1.5s 'steer queued' flash with a persistent dim list of SteerQueue contents above the input (cap 3 + '+N more'), removing entries when EngineEvent::Steer confirms delivery.
  _evidence: Claude Code components/PromptInput/PromptInputQueuedCommands.tsx_
  _shipped: dashboard.rs `queued_lines()` + `DashState.queued` — Enter mirrors the steer into `queued` alongside the live SteerQueue, and it stays on screen as a dim (COLD) `⏳ <preview>` row above the input until `EngineEvent::Steer` confirms delivery, which removes the oldest text match (matching by text, not index, since the engine may drain several at once and duplicate steers are legitimate). Capped at QUEUE_SHOWN (3) with a `+N more` tail, and `bottom_height()` grows the bar to fit so the list can never eat the crucible. The 1.5s FLASH constant and `flash_until` are gone: a smith only reads its queue at a turn boundary, so a steer sent mid-tool-call can wait minutes and the flash had long since vanished. Tests: `the_queued_list_caps_at_three_with_a_more_tail`, `a_delivered_steer_leaves_the_queued_list`, `an_unrelated_steer_confirmation_leaves_the_queue_alone`, `the_bottom_bar_grows_to_fit_the_queued_list`, `enter_pushes_input_into_steer_queue_and_flashes`._

## Wave 3

- [x] **42. Add LLM summarization compaction with the 9-section template** (L)
  engine/compact.rs stage two: when stub-pruning cannot reach budget, call the provider with a Rust port of BASE_COMPACT_PROMPT (9 fixed sections, <analysis> scratchpad stripped before reuse), replace the head with one user message containing the summary, keep the recent tail. Wire into agent.rs chat_shrinking before giving up.
  _shipped: engine/compact.rs `summarize()` + `BASE_COMPACT_PROMPT` (Rust port of the 9-section template) with `strip_analysis()` removing the `<analysis>` scratchpad; the head is replaced by one user message and the recent tail kept verbatim. Wired into agent.rs `chat_shrinking` before giving up._
  _evidence: Claude Code src/services/compact/prompt.ts (BASE_COMPACT_PROMPT); services/compact/compact.ts:387-520_
- [x] **43. Lead text-only LLM calls with a consequence-first NO-TOOLS preamble** (S)
  engine/judge.rs SYSTEM_PROMPT and the compact summarizer: send with no tools in the request AND lead with 'Respond with TEXT/JSON ONLY — tool calls will be REJECTED and waste your only turn' plus a matching trailer. Measured fallback rate drop 2.79% -> 0.01%; reduces slag's malformed-JSON judge retries.
  _shipped: engine/compact.rs `NO_TOOLS_PREAMBLE` / `NO_TOOLS_TRAILER`, used by `summary_request()` and by engine/judge.rs around its SYSTEM_PROMPT; both send `tools: vec![]`. Asserted in the judge tests._
  _evidence: Claude Code src/services/compact/prompt.ts:12-26,269-272_
- [x] **44. Wrap compaction summaries in a resume-silently continuation message** (S)
  engine/compact.rs summarizer path: deliver the summary as 'continued from a previous conversation that ran out of context — pick up the last task as if the break never happened, do not acknowledge the summary', with a pointer to slag's JSONL event log for exact earlier output.
  _shipped: engine/compact.rs `CONTINUATION_PREFIX`: the summary is delivered as "continued from a previous conversation ... pick up the last task exactly where it left off, as if the break never happened — do not acknowledge"._
  _evidence: Claude Code src/services/compact/prompt.ts:337-374_
- [x] **45. Retry an overflowing compaction call by dropping oldest rounds** (S)
  engine/compact.rs summarizer path: on context-overflow from the summary call (reuse is_context_overflow in agent.rs), drop oldest rounds sized to the gap (fallback 20%) and retry ≤3 before failing.
  _shipped: engine/compact.rs `overflow_gap_chars()` + `drop_oldest_rounds()`: a context-overflow from the summary call drops oldest whole rounds sized to the parsed gap and retries, failing after three. Covered by `overflowing_summary_call_drops_oldest_rounds_and_retries` and `summary_overflow_fails_after_three_drop_retries`._
  _evidence: Claude Code src/services/compact/compact.ts:227-292,449-490_
- [x] **46. Track read-file state and re-inject top files after compaction** (M)
  engine/tools.rs records path+timestamp on every read/edit into a per-session FileState map; engine/compact.rs summarizer path re-reads the 5 most recent files (capped) and appends them as a system-reminder after the summary, skipping files whose reads survive in the tail.
  _shipped: engine/tools.rs `FileState::seq` (a session-wide touch counter bumped on every read, write, and edit) feeds engine/compact.rs `summarize(.., file_context)`, which re-injects the most recent files as a system-reminder after the summary; `files_not_in_tail()` skips files whose live reads survive in the tail (stubbed tail reads do not count)._
  _evidence: Claude Code src/services/compact/compact.ts:122-129,1399-1464_
- [x] **47. Detect externally-changed files and inject diff reminders** (M)
  engine/agent.rs turn loop: stat tracked FileState files before each provider call; on mtime change inject '<system-reminder>file X was modified externally — this change was intentional, don't revert it: <diff snippet>'. Evict only on ENOENT. High leverage with MAX_ANVILS=3 parallel smiths touching shared files.
  _shipped: engine/tools.rs `ToolBox::external_changes()` + a `baselines` map (last content seen per tracked file, capped at 64KB, kept beside `read_state` so `FileState` stays Copy) and `line_diff()`, which trims the common head and tail and renders through the existing `diff_summary`. engine/agent.rs calls it at the top of every turn and injects the notes via `inject_reminders()` — the same carrier rule steers use, so several notes coalesce into one `<system-reminder>`. Reported once (the baseline re-stamps as it reports), a touch that leaves bytes alone is not a change, and ENOENT evicts from tracking instead of reporting a phantom diff._
  _evidence: Claude Code src/utils/attachments.ts:2063-2161; src/utils/messages.ts (case 'edited_text_file')_
- [x] **48. Switch to a fallback model after consecutive capacity errors** (M)
  config.rs gains fallback_model; agent.rs catches a Capacity-class error after N attempts and retries the turn on the fallback — or passes OpenRouter's native models:[primary,fallback] routing array in provider.rs build_body, which is nearly free; emit an event so the dashboard shows the switch.
  _shipped: config.rs `fallback_model()` (`SLAG_MODEL_FALLBACK` / `fallback_model`, off by default) read into provider.rs, which takes the item's cheaper branch: OpenRouter's native `models: [primary, fallback]` routing array in `build_body`, so a saturated primary sheds to the fallback inside one round trip with no client-side retry turn. The response's `model` field names whichever answered._
  _evidence: Claude Code src/services/api/withRetry.ts:326-364; src/query.ts:893-951_
- [x] **49. Add unattended persistent-retry mode with chunked heartbeat sleeps** (M)
  SLAG_UNATTENDED_RETRY config: 429/529 retry indefinitely with backoff capped at 5min and a ceiling; chunk long sleeps into 30s slices each emitting an ApiRetry heartbeat so dashboard and logs stay alive; wait until rate-limit reset timestamps instead of polling. Slag IS an unattended orchestrator.
  _shipped: config.rs `unattended_retry()` (`SLAG_UNATTENDED_RETRY` / `unattended_retry`) read into provider.rs as `unattended`: 429/529 stop consuming the attempt budget, backoff caps at 5 minutes under a 6h ceiling, and every long wait sleeps in 30s slices emitting one `ApiRetry{attempt, status, remaining_secs}` heartbeat per slice so the dashboard countdown and the JSONL log stay alive. A server-sent `Retry-After: 0` is floored so the accumulated wait strictly grows and the ceiling is reachable._
  _evidence: Claude Code src/services/api/withRetry.ts:91-104,433-506,814-822_
- [x] **50. Give side-LLM calls a minimal retry policy** (S)
  engine/provider.rs: add RetryPolicy(attempts, persistent?) on ChatRequest; forge strikes get full retries while judge.rs duel judging, recipe suggestions, and assay summaries get 0-1 attempts, so a capacity event never multiplies load across MAX_ANVILS workers.
  _evidence: Claude Code src/services/api/withRetry.ts:57-89,316-324_
  _shipped: `RetryPolicy { attempts, persistent }` (`engine/mod.rs`) with `full()`/`side()` rides on `ChatRequest.retry`; `provider.rs plan_retry()` takes it as a param, reading `policy.attempts` in preference to `self.max_attempts` and gating the free unattended capacity branch on `policy.persistent`. `judge.rs` (both call sites) and `compact.rs summary_request()` take `side()`; the agent's forge strike takes `full()`. The spec's "recipe suggestions" clause has no site — `engine/recipes.rs` constructs no ChatRequest. Tests: `a_side_policy_request_takes_one_swing`, `a_full_policy_request_rides_the_provider_budget`, `a_side_policy_call_does_not_wait_out_a_capacity_event_unattended`._
- [x] **51. Recover from output-token caps: silent escalation then continuation** (S)
  engine/agent.rs: the single 'continued' flag on FinishReason::Length (agent.rs:171) becomes: retry once with raised max_tokens, then up to 3 continuation nudges using CC's proven 'resume directly — no apology, no recap' text, then fail with a clear reason.
  _shipped: engine/agent.rs `FinishReason::Length` ladder: one silent escalation that discards the partial and retries at `RAISED_MAX_TOKENS`, then up to `MAX_CONTINUE_NUDGES` continuation nudges keeping the partial and appending `CONTINUE_NUDGE`, then a clear failure._
  _evidence: Claude Code src/query.ts:164,1185-1256_
- [x] **52. Parse context-overflow 400s and shrink max_tokens before compacting** (M)
  engine/agent.rs (context-error substrings at agent.rs:47-48): parse 'A + B > C' numbers from the provider 400 body, retry once with max_tokens = limit - input - buffer; fall through to compact.rs only if that fails — cheaper than compaction when only the output budget overflowed.
  _shipped: engine/agent.rs `shrunk_output_cap()` parses the provider's "input length and max_tokens exceed context limit: A + B > C" numbers out of the 400 body and retries the same request once at `max_tokens = limit − input − OUTPUT_SHRINK_BUFFER`, leaving history untouched; compaction only runs if that retry fails too. Covered by `output_only_overflow_shrinks_max_tokens_before_compacting`. (compact.rs `overflow_gap_chars()` parses the same shape for item 45's drop sizing — a different consumer of the same 400 body.)_
  _evidence: Claude Code src/services/api/withRetry.ts:384-427,550-595_
- [x] **53. Make request timeouts configurable and rebuild stale connections** (S)
  engine/provider.rs: REQUEST_TIMEOUT_SECS=600 becomes SLAG_API_TIMEOUT_MS (default ~300s); on reqwest is_connect/reset errors, rebuild the Client with pool_max_idle_per_host(0) for the retry. The 90s idle-watchdog half applies when slag adds SSE streaming.
  _shipped: engine/provider.rs `SLAG_API_TIMEOUT_MS` overriding the ~300s default via `parse_timeout_ms()`, and a Client rebuilt with `pool_max_idle_per_host(0)` on connect/reset errors so a stale pooled connection is not retried._
  _evidence: Claude Code src/services/api/claude.ts:795-811,1868-1928; withRetry.ts:112-118,218-230_
- [x] **54. Replace string provider errors with a typed taxonomy** (M)
  error.rs: SlagError::Provider(String) becomes {status, category, retryable, excerpt} classified once in provider.rs; events.rs includes the category in JSONL; dashboard shows 'credit balance low' or 'invalid key' instead of a raw body excerpt.
  _shipped: error.rs `ProviderErrorCategory` (Auth, Billing, RateLimit, Overloaded, ...) classified once in provider.rs and rendered as operator text — "invalid key", "credit balance low" — instead of a raw body excerpt._
  _evidence: Claude Code src/services/api/errors.ts:1040-1182_
- [x] **55. Discard partial turn state before any retry** (S)
  engine/agent.rs: on fallback-model or compact-retry paths, rebuild the outgoing message vec from last committed state instead of appending; audit run_with_compaction so a failed attempt's partial assistant/tool messages never leak into the retried request; mirror in duel.rs round retries.
  _evidence: Claude Code src/query.ts:712-741,899-919_
  _shipped: `agent.rs` takes a `committed = messages.len()` watermark before each model call; the silent-escalation retry now `truncate(committed)`s, so the invariant holds by construction instead of by luck of statement order. The continuation-nudge retry appends past the mark on purpose — the model needs its own partial to resume from. Two clauses of the spec have no site and were verified rather than built: slag has no fallback-model path (the analogue is the escalation/nudge/overflow-shrink retries), and duel rounds build fresh worktrees, prompts, and smiths per round, so no message vec crosses a round boundary. `chat_shrinking` needed no watermark: it sends `messages.clone()` and only ever rewrites deliberately (prune, summarize), never appends. Tests: `an_escalated_retry_re_sends_the_committed_history_not_the_partial`, `the_nudge_path_still_keeps_its_partial_on_purpose`._
- [x] **56. Thread a CancellationToken with reasons into tools and provider** (M)
  engine/agent.rs: CancelFlag (checked only at turn boundaries) becomes a tokio CancellationToken threaded into ToolCtx and provider chat futures via select!, with reason enum (UserAbort vs SteerInterrupt) — ctrl-C kills a 10-minute bash proof immediately, and steer-interrupts skip the error path.
  _evidence: Claude Code src/utils/handlePromptSubmit.ts:331; src/query.ts:1044-1051,1496-1515_
  _shipped: `engine/mod.rs` `CancelReason` (UserAbort / SteerInterrupt, each with a `label()`) and `Cancellation { flag, reason }`, which pairs the reason with the existing flag rather than replacing it — `CancelFlag` stays the bare `Arc<AtomicBool>` its four setters and two tests already hold, and `Cancellation::from_flag` is the bridge for them. First reason wins, so a steer arriving after a ctrl-C cannot relabel an operator abort. `ToolBox::with_cancel` threads the flag into tool dispatch: `run_shell` now races its wait against `cancelled()` in a `select!` and SIGKILLs the whole child process group on interrupt, so ctrl-C ends a ten-minute bash proof immediately instead of at the next turn boundary. `ForgeAgent::with_cancellation` takes flag and reason together and hands the toolbox the flag; `with_cancel` stays as the bridge for a bare flag and reads as `UserAbort`, the conservative default. `check_cancel` is the reason's production consumer and implements the spec's second clause: a `SteerInterrupt` narrates its label instead of emitting `EngineEvent::Error`, so a redirect does not read as a failure, while a ctrl-C keeps the error path. Live gap, stated rather than papered over: no surface raises `SteerInterrupt` yet — the dashboard's Ctrl-C is an abort and steer text rides the `SteerQueue`, never the cancel flag — so today the branch is a capability waiting on a caller, not a live path. Deviation: the flag is polled at `CANCEL_POLL_MS` (100ms) rather than awaited on a `CancellationToken` — the four surfaces that set it write a bare AtomicBool and none of them signal, so a token would have meant rewriting all four in the same change. Tests: `a_running_shell_dies_when_the_interrupt_is_raised`, `a_shell_without_a_cancel_flag_runs_normally`, `the_first_raised_reason_wins`, `a_steer_interrupt_reads_differently_from_an_abort`, `clones_share_the_flag_and_the_reason`, `an_adopted_flag_is_the_same_flag`., `a_steer_interrupt_narrates_instead_of_erroring`, `an_operator_abort_still_reports_an_error`, `a_bare_flag_still_reads_as_an_abort`._
- [x] **57. Teach the founder to brief ingots like zero-context colleagues** (M)
  pipeline/founder.rs: ingots ARE zero-context subagent prompts. Add the rules — goal + why + what's ruled out + concrete file paths; lookups get exact commands, investigations get the question; never delegate understanding. Directly raises first-heat pass rate.
  _shipped: pipeline/founder.rs zero-context briefing rules appended to the founder prompt: every `:work` briefs like a colleague joining with zero context, because the smith forging an ingot sees only that ingot._
  _evidence: Claude Code src/tools/AgentTool/prompt.ts (writingThePromptSection)_
- [x] **58. Use per-filetype bytes-per-token ratios in estimates** (S)
  engine/tools.rs truncation budgets + compact.rs estimator: 2 bytes/token for .json/.jsonl (dense punctuation tokenizes worse), 4 otherwise; flat-cost any base64 image content instead of counting chars. Underestimates let oversized results slip in and trigger compaction too late.
  _shipped: engine/compact.rs `density_weight()` + `message_chars()` feeding `convo_chars()`: a .json/.jsonl tool result reports twice its char count (2 bytes/token vs 4), resolved from the owning tool call's path via the existing `result_meta`, and each attached image costs a flat `IMAGE_FLAT_CHARS` instead of its base64 length. engine/tools.rs `dense_divisor()` halves the char cap in `recent_file_snapshots` for the same file types, so a re-injection budget is not quietly spent at double the token rate._
  _evidence: Claude Code src/services/tokenEstimation.ts:215-224,400-423_
- [x] **59. Label instruction-file provenance and warn on oversized files** (S)
  engine/prompt.rs stable band: when loading AGENTS.md/CLAUDE.md/recipes, emit 'Contents of <path> (project instructions, checked in):' labels under an OVERRIDE header, and log a dashboard warning when any single file exceeds a char cap.
  _evidence: Claude Code src/utils/claudemd.ts:89-92,1132-1204_
  _shipped: `flux.rs` `load_instruction()` renders every instruction file as `Contents of <path> (project instructions, checked in):` under `INSTRUCTION_OVERRIDE_HEADER`, a new `=== PROJECT INSTRUCTIONS ===` block in the forge flux that states the files override default behavior and that their contents are data, never operator authority. Both load sites take it: `prepare_flux` (BLUEPRINT, AGENTS.md, PLAN.md) and `prepare_resmelt_flux` (BLUEPRINT, PLAN.md). A missing file keeps its bare fallback and no label — no path to attribute. `oversized_instructions()` reports any file past `INSTRUCTION_BYTE_CAP` (40_000 bytes) as `EngineEvent::Warning`, emitted once at `forge::run` start rather than once per strike; it reads `metadata().len()` only, so the size question never costs a second read. The spec named `prompt.rs`'s stable band; the labels live in flux instead because they are per-run data and the stable band must stay byte-stable (`stable_band_is_byte_stable_across_runs`). Tests: `an_instruction_file_carries_its_path_and_an_override_header`, `a_missing_instruction_file_is_not_labelled`, `oversized_instruction_files_are_named_with_their_size`, `the_forge_flux_declares_the_override_contract`._
- [x] **60. Annotate injected blueprints and recipes with human-readable age** (S)
  engine/prompt.rs + engine/recipes.rs: stat mtime when injecting BLUEPRINT.md, PROGRESS.md history, or recipes and prepend 'written N days ago — verify against current code' for stale ones. Models are poor at date arithmetic; raw ISO timestamps don't trigger staleness reasoning.
  _shipped: `flux.rs` `file_age_days()` + `age_note()` are the one age vocabulary. Injected BLUEPRINT/AGENTS.md/PLAN.md carry `written N days ago, verify against current code` inside their provenance label; `recipe_view` (engine/tools.rs) appends the same note to its `[recipe meta — ...]` line. Same-day writes get no note: the annotation flags drift, and `written 0 days ago` would be noise on every run. Two deviations from the spec, both deliberate. The recipe age is stamped at injection, not in `recipes.rs`'s index: that index string is cached against the manifest mtime, so a `days ago` baked into it would freeze on the day the cache was written — the memo to reuse the manifest's existing `mtime_ns` would have bought exactly that bug. PROGRESS.md is injected as a 25-line tail (`read_tail`), an excerpt of a ledger appended to continuously, so its file mtime says nothing about the age of the lines shown and it carries no note. No stable-band edit was needed: `prompt.rs:177-184` already names staleness notes as arriving wrapped in `<system-reminder>`. Tests: `a_stale_instruction_file_is_annotated_with_its_age`, `a_fresh_instruction_file_carries_no_age_note`, `age_phrasing_reads_naturally_at_one_day`, `recipe_view_annotates_a_stale_recipe_with_its_age`, `recipe_view_happy`._
  _evidence: Claude Code src/memdir/memoryAge.ts:1-53_
- [x] **61. Add a live spinner status line: verb, elapsed, tokens, rate** (M)
  progress.rs (stream mode) + dashboard.rs bottom bar: fold EngineEvent::Tokens deltas plus a turn-start Instant into '⚒ Forging… (12s · 4.1k tok · 38 tok/s · esc to interrupt)'; metallurgical verb list in tui.rs; guard the rate readout (>5s elapsed, >2000 tok) against noisy early samples.
  _evidence: Claude Code components/Spinner.tsx:216-279; constants/spinnerVerbs.ts_
  _shipped: progress.rs `spinner_status()` + `LiveStatus` (turn-start Instant folding Tokens deltas), rendered by dashboard.rs `forge_status()` in the bottom bar with the verb from tui.rs `forge_verb()`. The tok/s segment is suppressed by `token_rate()` until RATE_MIN_ELAPSED (5s) and RATE_MIN_TOKENS (2000) both pass, so an early probe never reads as a wild rate. Checkbox flipped after reading the code against the spec. Tests: `token_rate_guards_noisy_early_samples`, `spinner_status_hides_the_rate_until_the_sample_is_trustworthy`, `spinner_status_reads_like_the_claude_code_line`, `live_status_folds_token_deltas_from_a_turn_start`, `forge_status_folds_turn_tokens_and_elapsed_into_the_live_line`, `bottom_bar_shows_the_spinner_status_while_forging`._
- [x] **62. Render word-level diffs with a 40% change-ratio fallback** (M)
  New src/render/diff.rs using the `similar` crate (word granularity + ratio check, fall back to full-line coloring above 40% change), used by dashboard.rs expanded view and progress.rs when showing anvil edit results — slag currently prints raw previews only.
  _evidence: Claude Code components/StructuredDiff/Fallback.tsx (CHANGE_THRESHOLD=0.4)_
  _shipped: new src/render/diff.rs on the `similar` crate — `diff_lines()` walks line ops (keeping a replacement together as a pair), then re-diffs each replaced pair by word so only the span that moved is highlighted. `change_ratio()` measures `(deleted + inserted) / (len(old) + len(new))`; scoring against one side counts a replacement twice and pushes ordinary one-word renames over the bar. At or above CHANGE_THRESHOLD (0.4) the pair falls back to plain full-line coloring, because past that a word diff is confetti. `absorb_inner_gaps()` folds the whitespace token `from_words` emits between two changed words into one highlighted run. The module is terminal-free — it returns kinds and spans — and dashboard.rs `diff_line_to_ratatui()` maps them onto the palette (removals WARM, additions HOT, changed spans BRIGHT+bold). Tests: `a_one_word_rename_highlights_only_that_word`, `a_rewritten_line_falls_back_to_full_line_coloring`, `the_threshold_is_where_the_fallback_starts`, `adjacent_changed_words_merge_into_one_run`, `pure_insertions_and_deletions_keep_their_markers`, `identical_text_is_all_context`, `multibyte_text_survives_word_granularity`, `empty_sides_do_not_panic`._
- [x] **63. Collapse tool results to one-liners with counts, ctrl-o to expand** (M)
  engine/events.rs: add line/byte counts and duration to ToolResult events; dashboard.rs feed shows '✓ read_file (43 lines · 0.3s)' and a ctrl-o toggle swaps to full previews; one hint in the HINT bar, not per line.
  _evidence: Claude Code components/messages/CollapsedReadSearchContent.tsx:276-290_
  _shipped: engine/mod.rs `EngineEvent::ToolResult` gains `lines`/`bytes`/`ms` (all `#[serde(default)]`, so JSONL written before they existed still deserializes), measured in engine/agent.rs on the FULL output rather than the truncated preview — the point of the counts is to say how much the collapsed line hides. dashboard.rs `result_counts()` renders `✓ read_file (43 lines · 1.5kB · 300ms)`, sub-second durations in ms since `0.0s` says nothing; failures stay inline with their error text, which is the one result worth reading uncollapsed. Ctrl-O flips `DashState.expanded` and `detail_lines()` swaps in the full preview — routed through render::diff (item 62) when `diff_sides()` finds a unified-diff shape. The hint lives once in HINT, never per feed line. Tests: `a_collapsed_result_says_what_it_is_hiding`, `ctrl_o_toggles_the_expanded_feed`, `the_expand_hint_appears_once_in_the_hint_bar`, `a_tool_result_keeps_its_preview_as_expandable_detail`, `a_unified_diff_preview_renders_through_the_word_differ`, `plain_output_is_not_mistaken_for_a_diff`._
- [x] **64. Show a context-percent gauge with compaction-buffer accounting** (M)
  engine/compact.rs knows the window and trigger threshold: emit a ContextGauge event; dashboard.rs bottom bar appends 'ctx 48%' coloring WARM→HOT near the compaction trigger; also emit the percentage in JSONL for headless runs; a 'ctx' steer keyword prints the breakdown.
  _evidence: Claude Code src/services/compact/autoCompact.ts:93-145; components/ContextVisualization.tsx_
  _shipped: `EngineEvent::ContextGauge` (engine/mod.rs) emitted once per turn from agent.rs after compaction runs, so the reading describes what the model is about to see. `context_pct()` measures against the compaction trigger, not the raw window — the budget is already net of output reserve, so 100% means "next turn prunes", and it saturates rather than wrapping. dashboard.rs `ctx_color()` goes COLD→HOT at 66 and HOT→WARM at 85; the `ctx` keyword typed into the steer box is answered locally by `ctx_breakdown()` (used / budget / remainder, digit-grouped) without costing a turn. The headless narrator folds the percent into the ingot footer and only above `CTX_LOUD` (66), so short runs stay quiet; the JSONL sink records every reading regardless. Tests: `gauge_updates_the_bar_without_spending_a_feed_line`, `gauge_heats_up_as_compaction_nears`, `ctx_breakdown_names_the_remainder_and_groups_digits`, `gauge_stays_silent_until_compaction_is_in_sight`._
- [x] **65. Send terminal notifications on finish/error, gated on idleness** (M)
  New tui.rs notify() writing BEL + OSC 9/99/777 by TERM_PROGRAM detection, called from pipeline/forge.rs on assay completion and Error/Finish events; dashboard.rs tracks last-keypress Instant and only notifies after 6s of user idleness.
  _evidence: Claude Code ink/useTerminalNotification.ts; hooks/useNotifyAfterTimeout.ts_
  _shipped: tui.rs `notify()` — BEL always, then the richest OSC the terminal takes (`notify_proto()` picks 9/99/777 by TERM_PROGRAM/TERM), called from pipeline/forge.rs on completion, crack, and budget exhaustion. Gated by `idle_enough(last_user_activity(), now, NOTIFY_IDLE)` at 6s, so a user still typing is never pinged; headless runs record no activity and always notify. Checkbox flipped after reading the code against the spec. Tests: `notify_proto_detects_terminals`, `notify_sequence_writes_bel_plus_the_right_osc`, `notify_sequence_strips_injection_bytes_from_text`, `idle_gate_blocks_active_users_and_passes_headless_runs`, `mark_user_activity_records_a_recent_keypress`._
- [x] **66. Substitute $ARGUMENTS, $0..$n, and named args into recipes** (S)
  recipes.rs: `slag recipe run <name> -- args` and in-conversation invocation substitute $ARGUMENTS/$0..$n/named placeholders into RECIPE.md before injection (shell-words crate); append 'ARGUMENTS: ...' when no placeholder exists. Turns static recipes into parameterized ones.
  _shipped: engine/recipes.rs argument substitution: `$ARGUMENTS` (all positionals joined by single spaces), `$0..$n`, and named args, with an `ARGUMENTS: ...` trailer appended when a recipe declares none so the invocation is never silently dropped._
  _evidence: Claude Code utils/argumentSubstitution.ts_

## Wave 4

- [x] **67. Persist per-ingot transcripts as JSONL and resume mid-ingot** (L)
  New engine/transcript.rs: agent.rs appends each ChatMessage + tool outcome to logs/transcripts/<ingot>-h<heat>.jsonl; forge.rs, on finding a Molten ingot with a transcript at restart, reloads messages and resumes the agentic loop at the recorded turn instead of resetting to Ore and burning a heat. Emit a compact_boundary line when compaction fires so resume loads only post-boundary context.
  _shipped: engine/transcript.rs `TranscriptWriter` + pipeline/forge.rs `resume_crashed_ingot` (landed with the wave-4 transcript work; checkbox flipped after reading the code, not re-implemented): agent.rs appends each ChatMessage and tool outcome to `logs/transcripts/<ingot>-h<heat>.jsonl`; a Molten ingot found with an open transcript at restart reloads its messages and resumes the loop at the recorded turn, `MAX_RESUME_ATTEMPTS = 3` bounding the retries, and no heat is burned. `compact_boundary` marks where compaction fired so resume loads only post-boundary context. Covered by engine/transcript.rs `resume_loads_only_post_boundary_context` and `begin_truncates_a_stale_transcript`._
  _evidence: Claude Code src/utils/sessionStorage.ts (recordTranscript, adoptResumedSessionFile); sessionStoragePortable.ts:480-735_
- [x] **68. Checkpoint files before edits and rewind on failed heats** (L)
  New anvil/checkpoint.rs hooked into engine/tools.rs write/edit dispatch: back up each file before first modification per attempt ({hash}@vN, mtime+size dedupe), snapshot at attempt start; on proof failure + heat retry (forge.rs) rewind the workspace to the attempt-start snapshot so retries start clean; add `slag rewind` to cli.rs. Outsized win for proof-gated retries.
  _shipped: anvil/checkpoint.rs `Checkpoint` + engine/tools.rs write/edit dispatch + cli.rs `rewind_workspace`/`show_rewind`: files are backed up before first modification per attempt as `{hash}@vN` with mtime+size dedupe, `begin()` snapshots at attempt start, and forge.rs rewinds to that snapshot on a failed heat. This node added the missing operator handle — bare `slag rewind` restores the most recent attempt, `--ingot` with `--heat` targets one by name, and a half-specified target is refused rather than guessed. Covered by cli.rs `rewind_restores_latest_attempt_and_reports_it`, `rewind_targets_a_named_attempt`, `rewind_reports_when_no_checkpoint_exists` and `rewind_refuses_a_half_specified_target`._
  _evidence: Claude Code src/utils/fileHistory.ts:86-347,748_
- [x] **69. Add a lifecycle hook engine with exit-code protocol** (L)
  New engine/hooks.rs: config.rs [hooks] table keyed by event enum (pre_tool, post_tool, tool_error, stop, session_start, pre_compact, ingot_forged, ingot_cracked). agent.rs calls hooks around tool dispatch; forge.rs fires ingot/session events. Exit 0 stdout becomes model-visible context; exit 2 blocks and feeds stderr to the smith.
  _shipped: engine/hooks.rs: HookEvent (pre_tool, post_tool, tool_error, stop, session_start, pre_compact, ingot_forged, ingot_cracked) + the exit-code protocol in `classify()` -- 0 promotes stdout to model-visible context, 2 blocks with stderr, anything else is logged and ignored. `config::hook_entries()` reads the `[hooks]` table with the `mcp_servers()` shape. agent.rs `dispatch_hooked()` wraps tool dispatch; forge.rs `fire_ingot_hook()` fires the ingot events. A timed-out or unspawnable hook reports CODE_FAILED and never blocks, so a broken hook cannot wedge a forge._
  _evidence: Claude Code utils/hooks.ts:747,1453,2648; entrypoints/sdk/coreTypes.ts:25_
- [x] **70. Match hooks by exact name, pipe-alternation, regex, or wildcard** (S)
  hooks.rs matcher: three-tier match (exact, 'bash|edit' alternation via the /^[a-zA-Z0-9_|]+$/ fast path, regex fallback) against slag tool names; empty or '*' matches all; invalid regex logs and skips instead of panicking.
  _shipped: engine/hooks.rs `matches()`: three tiers cheapest-first -- empty/`*` takes everything, the `^[a-zA-Z0-9_|]+$` charset takes the exact/alternation fast path (`bash|edit_file`), and anything else compiles as a regex. An invalid pattern warns once per process (`warn_once`) and selects nothing, so a config typo costs the hook, never the forge._
  _evidence: Claude Code utils/hooks.ts:1338-1380_
- [x] **71. Support per-hook timeout, once, async, and asyncRewake** (M)
  hooks.rs runs each hook under tokio::time::timeout with per-hook override; `once` removes from the session list after first run; async backgrounds without blocking; asyncRewake maps onto slag's steer channel — on exit 2 push a steer message into the smith's queue.
  _shipped: engine/hooks.rs: `timeout=` runs each hook under `tokio::time::timeout`; `once=` claims a single run per hook name via a process-wide spent set; `async=` backgrounds the hook so the smith never waits; `asyncRewake=` (which implies async) routes a late exit 2 onto the existing `SteerQueue` as a steer message, reaching the smith at its next turn instead of blocking a call that already ran._
  _evidence: Claude Code schemas/hooks.ts; utils/hooks.ts:166-268_
- [x] **72. Snapshot hook config at session start with a kill-switch** (S)
  hooks.rs loads config once in Engine::new and stores an immutable snapshot; config.rs adds disable_all_hooks. Guard rail matters because forge.rs lets smiths edit the workspace — a smith writing slag.toml must not register a hook into its own running session.
  _shipped: engine/hooks.rs `HookSnapshot` behind a `OnceLock`, loaded from config on first `snapshot()` call and frozen thereafter (slag has no `Engine` struct; forge start is the equivalent moment, same as `engine/mcp.rs` spawning its servers). `config::disable_all_hooks()` reads the config key or `SLAG_DISABLE_HOOKS`; `select()` then returns nothing while still parsing, so `slag hooks list` can show what is muted. The freeze is the guard rail: forge.rs hands the workspace to smiths that can rewrite `slag.toml`, and without it a smith could register a hook into its own running session._
  _evidence: Claude Code utils/hooks/hooksConfigSnapshot.ts; utils/hooks.ts:286-300_
- [x] **73. Filter hooks with cheap in-process `if` preconditions** (S)
  hooks.rs: `if = "bash(cargo *)"` glob evaluated against serialized tool args before process::Command::spawn, so a formatter hook on edit never forks for unrelated calls. Direct win where MAX_ANVILS parallel smiths would multiply useless hook spawns.
  _shipped: engine/hooks.rs `precondition_holds()`: `if = "bash(cargo *)"` checks the tool name, then globs the raw argument string and every string leaf inside it, entirely in-process before `Command::spawn`. With `MAX_ANVILS` smiths in parallel this is what keeps a formatter hook from forking per unrelated call._
  _evidence: Claude Code schemas/hooks.ts (IfConditionSchema); utils/hooks.ts:1386-1440_
- [x] **74. Add LLM prompt hooks and agentic verifier hooks** (M)
  hooks.rs kind Prompt{prompt, model} routed through engine/judge.rs on a cheap OpenRouter model; kind Agent runs a one-ingot smith. Natural upgrade for proof.rs: ingots whose acceptance can't be a shell command get `:proof-judge "..."` verified by a prompt hook.
  _shipped: engine/judge.rs `rule(provider, model, instruction, payload) -> Ruling{block, reason}` — `assay`'s cheap sibling, sharing its no-tools framing, outermost-brace lenient parse, and one-retry-then-error discipline, but at `Effort::Low`: a gate fires on every matching event, so one costing more than the work it guards does not get used. `{"decision":"allow"|"block","reason":"..."}`, and an unrecognized decision is malformed rather than a silent allow. engine/hooks.rs `HookKind::Prompt{prompt, model}` calls it through `run_prompt` (block → exit 2 carrying the reason, allow → exit 0 with the reason as context); `HookKind::Agent{prompt, model}` runs a grade-1 `make_smith` through `run_agent`, which refuses on a leading `BLOCK:` line and otherwise routes the output through `classify` as context. `run_hook` resolves the model per kind — judge model for Prompt, `model_base` override for Agent. Both failure paths land on `CODE_FAILED`, never a block: a gate that cannot rule must not wedge a forge._
  _evidence: Claude Code schemas/hooks.ts (PromptHookSchema, AgentHookSchema); utils/hooks.ts:2224-2295_
- [x] **75. Let PreToolUse hooks rewrite tool input and inject context** (M)
  hooks.rs parses optional JSON from hook stdout; tools.rs applies updated_input before executing and appends additional_context to the tool result. Enables guard hooks that rewrite dangerous commands or inject lint output after edits without a model round-trip.
  _shipped: engine/hooks.rs `structured()` parses a hook's JSON stdout into `updated_input` / `additional_context` (both spellings); agent.rs `dispatch_hooked()` applies `HookRun::rewritten_arguments()` before `toolbox.dispatch` and appends the context to the tool result. Object-valued rewrites only -- a bare string would break the tool. Later hooks see the call as configured, so last-writer-wins on the rewrite._
  _evidence: Claude Code types/hooks.ts:70-107; services/tools/toolHooks.ts:348-452_
- [x] **76. Add HTTP hooks with env-var-allowlisted headers** (S)
  hooks.rs HTTP kind using the reqwest stack from provider.rs: POST event JSON to a URL; headers interpolate only variables named in allowedEnvVars (everything else empty) so config files can't exfiltrate secrets. Gives CI notification on ingot_cracked.
  _shipped: engine/hooks.rs `HookKind::Http{url, headers, allowed_env}` — `run_http` POSTs the payload JSON on provider.rs's existing reqwest stack (no new Cargo.toml line) under the hook's own timeout. `http_outcome` maps status onto the exit-code protocol using HTTP's own vocabulary rather than inventing a second one: 2xx → exit 0 through `classify` so a webhook can return context, 403 → exit 2 with the body as the reason (an empty body still names one, so a refusal is never silent), everything else → `CODE_FAILED`, because a notification endpoint that is down must not wedge a forge. `interpolate()` expands `$NAME`/`${NAME}` in header values only for names in `allowedEnvVars`; every other name resolves empty, so a config line cannot exfiltrate an unlisted variable by naming it. Built by a single left-to-right scan like the recipe span expander — an expanded value carrying `$OTHER` is text, never re-expanded on a second lap._
  _evidence: Claude Code schemas/hooks.ts (HttpHookSchema); utils/hooks.ts:2295-2330_
- [x] **77. Document and instrument hooks: metadata table plus progress events** (S)
  hooks.rs exposes a static EVENTS table (event, summary, stdin/exit-code contract) printed by `slag hooks list`; emit hook_started/hook_finished JSONL events with name, exit code, duration_ms; render optional status_message in the smith status line so slow hooks don't look like a hung smith.
  _shipped: engine/hooks.rs static `EVENTS: [EventDoc; 8]` (event, summary, stdin fields, what exit 2 refuses) rendered by `describe()` and printed by `slag hooks list` (cli.rs `show_hooks` + the `Hooks` arm appended at the end of the Command enum). `EngineEvent::HookStarted`/`HookFinished` carry name, hook_event, exit code and `duration_ms` into the JSONL stream; `status_message` renders in the dashboard feed so a slow hook reads as work rather than a hung smith, and events.rs prints only non-zero finishes to keep successful hooks out of the stream._
  _evidence: Claude Code utils/hooks/hooksConfigManager.ts:26-140; utils/hooks.ts:2104-2110,2240-2250_
- [x] **78. Execute inline !`cmd` shell spans in recipe bodies** (M)
  recipes.rs expansion pass runs !`cmd` spans and ```! blocks through the same bash executor tools.rs uses (inheriting sandbox/allowlist) and splices stdout. Recipes can embed live state: !`git status --porcelain`. Build the output string manually — never regex-replace with untrusted text.
  _shipped: engine/recipes.rs `expand_shell_spans(&ToolBox, body)`, called from `ToolBox::recipe_view` so a recipe embeds live state at injection time. One manual scanner shaped like `substitute_args`: literal segments and command stdout are pushed into an output `String` once and never revisited, so stdout carrying !`...` or `$0` lands inert (no regex, no str::replace with untrusted text). Both syntaxes — inline !`cmd` (trailing newline trimmed so the value sits mid-sentence) and a ```! fence run as one script (spliced verbatim). `\!` escapes the opener so a recipe can document the feature; an unterminated span or fence stays literal. Sandbox inheritance is by construction, not by copy: `recipes` is a child module of `tools`, so it calls the private `ToolBox::bash` directly and every span pays the destructive-command refusal, the [policy] deny/ask rules, and the sleep guard with no duplicated gate to drift. A refused or timed-out span splices `[slag: span failed: …]` and the rest of the recipe still expands; a non-zero exit arrives as bash's own `(exit N)` note; per-span output caps at 4 KiB through `truncate_middle` and per-span timeout is 30s._
  _evidence: Claude Code utils/promptShellExecution.ts_
- [x] **79. Emit a `slag status --json` contract for external consumers** (M)
  cli.rs: read the live JSONL event log plus persisted session costs and print one JSON object: run id, ingots by status, spend, tokens, context %, active anvils. Lets tmux statuslines, CI, and the website poll a forge without scraping the TUI.
  _evidence: Claude Code src/components/StatusLine.tsx:46-98_
  _shipped: cli.rs `status_json()` — one JSON object carrying run id (newest event-log stem), ingots by status, active anvils, spend (persisted `.slag/session-costs.json` preferred over the log sum), prompt/completion/total tokens, context tokens and pct, and last event. Reads the live JSONL with the no-parse scanners, never demands a key, and skips `run-*.jsonl` ledgers so a ledger append cannot masquerade as the newest event log. Checkbox flipped after reading the code against the spec. No covering test for the assembled contract: `status_json()` is never called by a test. `status_json_runs_and_ps_parse` reads like coverage but only asserts clap turns `slag status --json` into `Command::Status { json: true }`. The ingredients are each tested — `scanner_pulls_values_without_parsing`, `outcome_reads_the_last_terminal_event`, `session_costs_accepts_several_schemas`, `session_costs_never_reports_token_counts_as_dollars`, `run_ledgers_are_excluded_from_runs_and_newest_log` — so what is unguarded is the shape of the emitted object, which is exactly what an external consumer binds to._
- [x] **80. Make JSONL readers crash-tolerant: skip bad lines and truncated tails** (S)
  engine/transcript.rs and any logs/ reader: parse per-line with serde, warn once and skip malformed lines, treat a non-newline-terminated last line as a partial write to drop — mirrors the guard crucible.rs already has for ingot lines.
  _shipped: engine/transcript.rs `read_jsonl_tolerant<T>()` (landed with the wave-4 transcript work; checkbox flipped now): per-line serde parse, malformed lines skipped with one warning per file, a last line without a trailing newline dropped as a partial write. Consumers: transcript resume + anvil/checkpoint.rs manifests + `slag insights`. The remaining logs/ surfaces are tolerant by construction — cli.rs status/runs use no-parse `"key":value` scanners, session files self-heal (parse failure = prune)._
  _evidence: Claude Code src/history.ts:106-143; src/utils/sessionStoragePortable.ts:735_
- [x] **81. Append run metadata as typed entries in the run JSONL** (S)
  events.rs sink gains a metadata entry type: forge start appends {run_id, git_branch, model, flux_profile, crucible_hash}; assay appends the final verdict — one self-describing log file per run, no sidecar files for the runs lister.
  _shipped: engine/events.rs `RunEntry::RunMeta` + flux.rs `profile()`: forge start appends one typed metadata line carrying run_id, started, git_branch, model, duel, flux_profile and crucible_hash; assay appends the verdict as the last line. This node added `flux_profile`, which labels the flux inputs live at forge start (`blueprint+alloy+crucible+ledger`, or `bare`), so two runs on one model stay distinguishable. One self-describing log file, no sidecars. Covered by engine/events.rs `run_log_is_one_self_describing_jsonl_file` and flux.rs `profile_names_present_inputs_in_fixed_order` / `profile_is_bare_when_no_input_has_content`._
  _evidence: Claude Code src/utils/sessionStorage.ts:2572-2815_
- [x] **82. List past runs from 64KB head+tail windows, never full parses** (M)
  New `slag runs` subcommand + dashboard run picker: stat + head/tail window reads per logs/*.jsonl with a no-parse '"key":"value"' scanner to pull start time, blueprint name, last event, and pass/fail — instant even with megabyte-scale event logs.
  _shipped: cli.rs `Runs` subcommand (landed with the wave-4 log work; checkbox flipped after reading the code, not re-implemented): `RUN_SCAN_WINDOW = 64 * 1024` head+tail window reads per `logs/*.jsonl` via `read_windows`, with no-parse `scan_num_sum`/`scan_num_last` `"key":value` scanners feeding `run_row` — a megabyte log is never fully parsed. Covered by cli.rs `list_runs_reads_windows_not_whole_files`, `scanner_pulls_values_without_parsing` and `list_runs_on_missing_dir_is_empty`. **Partial**: the spec's second half, a dashboard run picker over the same reader, is not built — `rg 'run_picker|list_runs' dashboard.rs` is empty. The scanner it would call is done and public, so the picker is a UI shell over `read_windows`; it was left out because dashboard.rs was contended by two other nodes this wave and a fourth writer would have cost a three-way conflict, not because the reader is missing._
  _evidence: Claude Code src/utils/listSessionsImpl.ts:83; sessionStoragePortable.ts (LITE_READ_BUF_SIZE)_
- [x] **83. Register live forges in a PID registry with `slag ps`** (S)
  main.rs writes ~/.slag/sessions/<pid>.json (pid, cwd, run id, phase, started_at), removed via the cleanup registry; `slag ps` in cli.rs lists live forges (pid-liveness checked) — and forge.rs can refuse to start a second forge on the same crucible directory.
  _shipped: cli.rs `sessions_dir`/`live_sessions_in`/`show_ps` + main.rs forge gate (landed with the wave-4 session work; checkbox flipped after reading the code, not re-implemented): each forge writes `~/.slag/sessions/<pid>.json`, dead pids are pruned on read, `slag ps` lists the live ones, and a second forge on the same crucible dir is refused. Covered by cli.rs `status_json_runs_and_ps_parse`._
  _evidence: Claude Code src/utils/concurrentSessions.ts:60_
- [x] **84. Print a copy-pasteable resume hint on interrupted exit** (S)
  forge.rs / main.rs: when exiting via Ctrl-C or provider failure with ore/molten ingots remaining, print 'resume with: slag forge' plus counts (N ore, M molten reset) after the ratatui teardown, so the operator knows the run is resumable.
  _shipped: main.rs `resume_hint()` (landed with the wave-4 session work; checkbox flipped after reading the code, not re-implemented): called after the ratatui teardown on the interrupted-exit path, so the hint survives the alternate-screen restore; prints the ore and molten-reset counts alongside a copy-pasteable `slag resume`. No unit test: main.rs carries no test module, and the mechanism is an ordering guarantee against the ratatui teardown that a unit test cannot observe. Verified by reading the call site at main.rs, which sits after the teardown on both the error and interrupt paths._
  _evidence: Claude Code src/utils/gracefulShutdown.ts:144_
- [x] **85. Persist steer history with buffered lockfile flush and recall** (M)
  dashboard.rs steer input: persist submitted steers to ~/.slag/history.jsonl (project field = cwd) with lockfile + bounded retries, load newest-first deduped for up-arrow recall across runs; flush through the cleanup registry so Ctrl-C never loses the last steer.
  _shipped: new module `steer_history.rs` + the steer-input path in dashboard.rs. `record()` buffers into a process-global `Mutex<Vec<Entry>>` so the keypress path never touches the disk; `flush()` claims `history.jsonl.lock` via `create_new` with 20×25ms bounded retries, appends one JSON object per line, and re-buffers rather than drops when a peer holds the lock. A lock older than `STALE_LOCK` (30s) is broken instead of waited on, so one crashed forge cannot wedge history forever. `recall()` filters to the cwd `project`, reverses newest-first, and dedupes keeping the newest, with unflushed entries prepended so a steer typed this run recalls immediately; a malformed line is skipped, not fatal. `install_flush()` rides the same `shutdown::register` registry as item 83's session cleanup, so Ctrl-C and the panic hook both land the last steer. `DashState` gained `history`/`history_pos`/`draft`: Up walks newest-first and holds at the oldest, Down past the newest restores the half-typed draft, and typing or Backspace ends the walk so an edit survives. Covered by steer_history.rs `flush_appends_jsonl_and_releases_the_lock`, `a_held_lock_gives_up_bounded_and_keeps_the_buffer`, `a_stale_lock_is_broken_not_waited_on`, `recall_is_newest_first_deduped_and_project_scoped`, `a_malformed_line_does_not_sink_the_rest_of_the_history`, and dashboard.rs `up_walks_the_recall_list_and_down_restores_the_draft`, `submitting_a_recalled_steer_resets_the_cursor_and_prepends_it`, `typing_after_a_recall_keeps_the_recalled_text_as_the_new_draft`._
  _evidence: Claude Code src/history.ts:190-329_
- [x] **86. Persist and restore run cost state across resume** (S)
  Persist .slag/session-costs.json keyed by run id from dashboard.rs state on exit/crack; flux.rs reloads it when re-melting so assay reports whole-job spend, not just the last invocation.
  _shipped: dashboard.rs `SESSION_COSTS`/`save_session_cost`/`load_session_cost` + flux.rs `spend_note` (landed with the wave-4 cost work; checkbox flipped after reading the code, not re-implemented): `.slag/session-costs.json` is keyed by run id and written on exit or crack, `persistable()` refusing the "default" placeholder; the re-melt prompt reloads it so assay reports whole-job spend rather than the last invocation's. Covered by dashboard.rs `session_costs_round_trip_and_replace_per_run` and cli.rs `session_costs_accepts_several_schemas`._
  _evidence: Claude Code src/cost-tracker.ts:87-175; src/costHook.ts_
- [x] **87. Split wall vs API vs tool duration, including retry overhead** (S)
  engine/agent.rs times each provider.chat() call (retries separately) and each ToolBox dispatch; assay.rs prints 'wall 4m12s · api 2m01s (retries +18s) · tools 1m40s'. Diagnoses whether slowness is model, retry storms, or proofs.
  _shipped: engine/mod.rs `stats` (landed with the wave-4 accounting work; checkbox flipped after reading the code, not re-implemented): `record_api(d, retry)` splits first-attempt time from in-session retry overhead, `mark_run_start` anchors the wall clock, `record_tools` sums tool batches, and `durations_line` renders `wall 4m12s · api 2m01s (retries +18s) · tools 1m40s` for assay.rs to print. Covered by engine/mod.rs `durations_line_splits_wall_api_retries_tools`._
  _evidence: Claude Code src/cost-tracker.ts:11-21,71-80,238-242_
- [x] **88. Track lines added/removed per ingot and per run** (S)
  engine/tools.rs edit/write handlers diff old vs new content and emit added/removed counts in ToolResult events; forge aggregates per ingot so assay shows churn per ingot and total.
  _shipped: engine/mod.rs `stats::line_churn`/`record_churn`/`churn_line` + engine/tools.rs write/edit handlers + pipeline/assay.rs: a multiset delta over lines counts added and removed on every successful write, credited to the forging ingot through the ToolBox's checkpoint key so parallel anvils never cross-attribute; assay prints `churn: +412/-87 (i2 +300/-12, i1 +112/-75)`, run total first, heaviest ingot next. Covered by engine/mod.rs `line_churn_counts_added_and_removed_lines`, `churn_line_leads_with_the_run_total_then_heaviest_ingot` and `churn_line_drops_redundant_detail_and_reports_nothing_for_a_clean_run`._
  _evidence: Claude Code src/cost-tracker.ts:54-56,241_
- [x] **89. Aggregate tool-error categories into the assay report** (S)
  forge.rs already sees ToolResult{ok:false} events: aggregate counts per tool name plus a coarse error class from the output prefix — 'tool errors: edit 7 (no-match 5), bash 2'. Directly surfaces when the fuzzy edit ladder is thrashing.
  _shipped: engine/mod.rs `stats::record_tool_error`/`classify_error`/`tool_errors_line` (landed with the wave-4 accounting work; checkbox flipped after reading the code, not re-implemented): failures bucket into no-match / multi-match / timeout / sandbox / stale-read / nonzero-exit / other, and assay.rs prints `tool errors: edit_file 7 (no-match 5), bash 2`, tools sorted by failure count. Covered by engine/mod.rs `tool_errors_line_ranks_tools_and_names_classes`._
  _evidence: Claude Code src/commands/insights.ts:240-299_
- [x] **90. Run idempotent startup migrations for model slugs and formats** (S)
  New migrations.rs called early in main.rs: rewrite deprecated OpenRouter model slugs in ~/.slag config and Slagfile (OpenRouter retires slugs regularly) and upgrade old crucible header formats — each migration a pure idempotent fn over config.rs types, no completion flags.
  _shipped: migrations.rs `run`/`rewrite_model_slugs`/`upgrade_crucible_header` (landed with the wave-4 migration work; checkbox flipped after reading the code, not re-implemented): called from main.rs before any project file is touched, rewriting deprecated OpenRouter model slugs and old crucible header formats in place; re-running changes nothing once applied. Covered by migrations.rs `slug_rewrite_is_idempotent` and `header_upgrade_is_idempotent`._
  _evidence: Claude Code src/migrations/migrateFennecToOpus.ts_
- [x] **91. Export runs as Chrome/Perfetto trace files** (M)
  Add `--trace trace.json` to forge: a second event sink beside spawn_jsonl_sink (engine/events.rs:62) mapping IngotStart/ToolCallStart/ToolResult/IngotDone to Chrome trace-event B/E pairs, one lane per anvil, args carrying tokens+cost. Open in ui.perfetto.dev to see where parallel forges serialize.
  _evidence: Claude Code src/utils/telemetry/sessionTracing.ts:1-80; perfettoTracing.ts_
  _shipped: render/trace.rs — `TraceMapper` turns IngotStart/Done into a B/E pair and nests ToolCallStart/ToolResult inside it, reading the `ms` item 63 already put on the result rather than re-timing. IngotDone `args` carry the tokens and cost burned since the previous one, accumulated from `Tokens` events and reset per ingot. The mapper is clock-free — the caller supplies each `ts` — so the tests are deterministic and the sink keeps one monotonic base. Lane assignment: each open ingot holds the lowest free lane and frees it on Done, so parallel forges read as parallel lanes and a serial run as a staircase down lane 1; since only ingot events carry an id, a tool call takes the sole open ingot's lane when exactly one is open and shared lane 0 otherwise, and results pair back to calls by name so two anvils interleaving on lane 0 still nest. `attach()` taps `hooks.events` — the one channel carrying both pipeline events (forge.rs) and agent events (fanned in at smith/native.rs) — teeing when the dashboard holds it and taking it outright when headless, so `--trace` works without `--tui`. The file is a JSON array, so the closing bracket decides whether a viewer loads it at all: the sink writes it on drain after synthesising an `E` for everything still open, and because the shell Ctrl-C path ends in `process::exit(130)` and never lets a task finish, `attach()` also registers a synchronous close with shutdown.rs (item 38's registry). A shared `AtomicBool` means exactly one of them writes it, and the sink claims the flag before its final writes — the sink's handle tracks its own offset, so writing on after a cleanup appended `]` would overwrite the bracket and undo the rescue. Tests: `ingot_maps_to_a_matched_begin_end_pair_on_one_lane`, `two_concurrent_ingots_take_separate_lanes`, `a_serial_run_reuses_the_first_lane_instead_of_drifting`, `tool_calls_nest_on_the_lane_of_the_only_open_ingot`, `tools_fall_back_to_the_shared_lane_when_two_anvils_are_open`, `interleaved_tools_on_the_shared_lane_pair_by_name`, `tool_result_carries_the_duration_measured_by_item_63`, `ingot_end_args_carry_the_tokens_and_cost_burned`, `usage_resets_between_ingots`, `close_open_ends_every_bar_a_ctrl_c_left_hanging`, `the_sink_writes_a_closed_json_array_a_viewer_can_load`, `a_run_cut_short_still_yields_a_loadable_trace`, `a_hard_exit_still_closes_the_array_from_the_shutdown_registry`, `closing_an_already_closed_trace_is_a_no_op`, `attach_headless_routes_events_straight_to_the_trace`, `attach_tees_so_the_dashboard_keeps_seeing_every_event`, plus cli.rs `trace_flag_takes_a_path_and_stays_off_by_default` and `trace_flag_works_on_resume_too`._
- [x] **92. Report forge progress via OSC 9;4 with tmux passthrough** (S)
  progress.rs: forged/total ratio → osc_progress(pct) after each IngotDone, error state on crack, cleared in restore_terminal(); copy CC's terminal allowlist (exclude Windows Terminal). Route all OSC emissions through a single tui.rs write_osc() that wraps in DCS passthrough when $TMUX/$STY is set.
  _evidence: Claude Code ink/terminal.ts:17-57; ink/termio/osc.ts:428-444 (wrapForMultiplexer)_
  _shipped: tui.rs `write_osc()` is the single funnel for every OSC emission — it gates on a real stderr terminal and wraps in DCS passthrough via `wrap_for_multiplexer()` when `detect_multiplexer()` sees `$TMUX`/`$STY` (tmux gets its inner ESCs doubled, screen takes the sequence raw). progress.rs `report_forge_state()` pushes `progress_state()` after each ingot event — the forged/total ratio, or an error pip once anything cracks — and `clear_forge_state()` wipes it from `restore_terminal()`. The allowlist in `osc_progress_capable()` is Claude Code's minus Windows Terminal. Checkbox flipped after reading the code against the spec. Tests: `wrap_for_multiplexer_builds_dcs_envelopes`, `detect_multiplexer_prefers_tmux_and_ignores_empty_vars`, `osc_progress_sequence_covers_set_error_clear_and_clamps`, `osc_progress_capable_follows_the_allowlist`, `progress_state_is_error_on_crack_and_ratio_otherwise`. `write_osc()` itself writes to stderr and has no direct test; every piece it composes does._
- [x] **93. Set the terminal title to live forge state, clear on exit** (S)
  tui.rs set_title() via OSC 0 (through the multiplexer wrapper): '⚒ slag 3/9 forging i4' updated on IngotStart/IngotDone in progress.rs; clear in dashboard.rs restore_terminal() and the panic hook.
  _evidence: Claude Code utils/gracefulShutdown.ts:122; ink/termio/osc.ts_
  _shipped: tui.rs `set_title()`/`clear_title()` emit OSC 0 through the same `write_osc()` multiplexer wrapper, with `osc_text_clean()` stripping control bytes so title text cannot smuggle its own escapes. progress.rs `forge_title()` builds `⚒ slag 3/9 forging i4` (plus a cracked count) and `report_forge_state()` refreshes it on IngotStart/IngotDone; dashboard.rs `restore_terminal()` clears it on every exit path, including the installed panic hook. Checkbox flipped after reading the code against the spec. Tests: `title_sequence_wraps_clean_text_in_osc_0` (OSC 0 form, the empty clear form, and control-byte stripping), `forge_title_carries_ratio_active_ingot_and_cracks`._
- [x] **94. Enable DEC 2026 synchronized output for flicker-free draws** (M)
  dashboard.rs draw loop: wrap each frame in crossterm's BeginSynchronizedUpdate/EndSynchronizedUpdate when the terminal qualifies (port CC's allowlist + tmux exclusion into tui.rs); reuse the DA1-sentinel probe trick if slag later queries capabilities.
  _evidence: Claude Code ink/terminal.ts:67-104; ink/terminal-querier.ts:1-40_
  _shipped: tui.rs `sync_output_capable()` ports Claude Code's allowlist and excludes every multiplexer (a passthrough would sync tmux's own redraw, not the inner pane), memoized behind `sync_updates_enabled()`; dashboard.rs wraps each frame in crossterm's BeginSynchronizedUpdate/EndSynchronizedUpdate when it returns true. Checkbox flipped after reading the code against the spec. Tests: `sync_output_capable_gates_on_terminal_and_multiplexer`._

## Wave 5

- [x] **95. Build a command policy engine: split compounds, strip wrappers, prefix rules** (L)
  New engine/policy.rs consumed by tools.rs bash: config-driven allow/deny lists in slag.toml (deny = ["git push:*", "curl:*"]), compound-split (&&, ;, |) so every subcommand must pass, iterative wrapper/env-prefix stripping, fail-closed on backticks/$( ) command substitution, deny > ask > allow precedence. Real guard rail beyond the path sandbox.
  _shipped: engine/policy.rs (hand-rolled, no regex): `[policy]` table via config.rs policy_entries() (deny/ask/allow, TOML-array or comma-list values), token-wise prefix rules with `:*` wildcard, compound-split on `&&`/`;`/`|`/`&`/newlines, iterative wrapper/env/numeric-prefix stripping incl. sh -c payloads, fail-closed on backtick/`$(` substitution while a policy exists, deny > ask > allow (ask refuses with add-an-allow-rule guidance — slag runs unattended). Wired in tools.rs bash() via ToolBox::with_policy; forge.rs + smith/native.rs attach Policy::from_config(). Empty policy checks nothing._
  _evidence: Claude Code src/tools/BashTool/bashPermissions.ts:524-566; utils/permissions/shellRuleMatching.ts; dangerousPatterns.ts_
- [x] **96. Classify provably read-only bash for concurrent scheduling** (L)
  engine/tools.rs path_access returns None for bash today, forcing every bash call to be treated as unscheduled. A trimmed classifier (ls/cat/rg/fd/git status/diff/log with safe-flag tables, excluding execution escapes like -exec and jq -f) lets agent.rs run read-only bash concurrently with readers and lets duels share a repo view safely.
  _shipped: tools.rs read_only_bash() + path_access "bash" arm: safe-command table (ls/cat/head/tail/wc/grep/diff/sort/…, rg minus --pre, find minus -exec/-execdir/-ok/-delete, fd minus -x/-X, jq minus -f, git status/diff/log/show/blame/rev-parse/branch minus mutating branch flags), env-assignment prefixes allowed, every compound segment must pass, fail-closed on backtick/`$(`/`>` and on any wrapper or unknown command. agent.rs plan_segments now batches read-only bash with readers (test updated)._
  _evidence: Claude Code src/tools/BashTool/readOnlyValidation.ts:35-90,1509_
- [x] **97. Support background bash with run_in_background and sleep guards** (L)
  engine/tools.rs + agent.rs: run_in_background arg spawns the process group detached with stdout redirected to logs/bg/<id>.log, returns id+path immediately, injects a completion note via the existing steer channel. The S-size subset — reject `sleep >2s` with 'background it instead' guidance — is worth doing alone.
  _shipped (S subset): tools.rs `blocked_sleep()` refuses a leading integer `sleep N` with N>=2 and names the remedy (raise `timeout`, curl --retry, `until` loop). Float durations, sub-2s pacing, `sleep 5 &`, and sleeps inside pipelines/loops/subshells pass. The bash spec teaches the rule. run_in_background still pending._
  _shipped (full): tools.rs bash gains `run_in_background` — destructive/policy gates still apply, sleep guard skipped (a detached sleep blocks nothing); spawn_background() runs `sh -lc` in its own process group (no kill_on_drop, no timeout) with stdout+stderr streamed to logs/bg/<id>.log and returns id+path immediately; a waiter task reaps the child and pushes "background job <id> exited …" into the agent's SteerQueue (ToolBox::with_steer, wired from hooks.steer in forge.rs + smith/native.rs — casts stay steerless). Spec teaches it; sleep refusal names it as remedy._
  _evidence: Claude Code src/tools/BashTool/BashTool.tsx:241,525-530,610_
- [x] **98. Grow recipe frontmatter: allowed-tools, model, fork context, paths gating** (L)
  recipes.rs Recipe struct gains allowed_tools, model, context (inline|fork), paths. `context: fork` spawns a sub-smith via smith/native.rs with only the recipe as its brief (forge.rs already runs parallel smiths); `paths` globs hide recipes until matching files are touched, cutting index tokens.
  _shipped: recipes.rs Recipe gains allowed_tools, model, context (inline|fork enum), paths — all three list keys share the comma/inline/dash-list grammar, quotes trimmed. paths gating live in render_index: a paths-bearing recipe hides until a workspace file matches (hand-rolled glob: `*` in-segment, `**` crossing, `?`; bare patterns match basenames; bounded walk skipping .git/target/node_modules); the snapshot cache is bypassed while any recipe is paths-gated. recipe_view prepends a `[recipe meta — …]` header naming allowed_tools/model/context. Out of scope: fork execution + allowed_tools enforcement — no recipe-bound session hook exists yet (NativeSmith.skill still reserved)._
  _evidence: Claude Code skills/loadSkillsDir.ts:185-263; types/command.ts_
- [x] **99. Add a minimal MCP stdio client to import external tools** (L)
  engine/tools.rs adds an mcp.rs adapter: spawn configured stdio servers (config.rs [mcp] table), initialize + tools/list at startup, expose each as a slag tool with mcp__server__tool naming; recipes' requires_tools works unchanged. Scope to stdio-only, no OAuth/HTTP transports.
  _shipped: engine/mcp.rs (newline-delimited JSON-RPC over the child's stdio), `[mcp]` table via section-aware parse_config_lines, `ToolBox::all_specs()` = natives + MCP, dispatch routes the `mcp__` prefix, prompt.rs feeds MCP names to the recipe gate, main.rs connects before the pipeline and prints what came up. Failed servers warn and are skipped._
  _evidence: Claude Code services/mcp/client.ts; services/mcp/config.ts_
- [x] **100. Build `slag insights`: offline analytics over run logs with cached facets** (L)
  New cli.rs subcommand over logs/*.jsonl: deterministic stats (ingots forged/cracked, heats per ingot, cost per ingot, duel margins, tool errors) plus optional cheap-model facet extraction per run cached as logs/facets/<run>.json. Turns the slag heap into a learning loop across projects.
  _shipped: src/insights.rs + `slag insights [--refresh]` (cli.rs/main.rs; needs no key or network): aggregates logs/run-*.jsonl ledgers (RunEntry gains Deserialize) for forged/cracked, per-ingot heats, and assay verdicts, and every other logs/*.jsonl engine stream for cost, prompt/completion tokens, per-tool error counts, and duel_verdict margins (n/a when none logged); per-log facets cache at logs/facets/<stem>.json — schema-stamped, reused while at least as new as the log, --refresh recomputes. Parsing rides item 80's tolerant reader, so garbled lines never sink the report. Scope call: facets stay deterministic — no cheap-model extraction, keeping the command offline and zero-spend._
  _evidence: Claude Code src/commands/insights.ts:260-273,430,941-970_
